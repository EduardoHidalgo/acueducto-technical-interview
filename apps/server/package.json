{
    "name": "server",
    "private": true,
    "type": "module",
    "version": "0.0.0",
    "scripts": {
        "dev": "php artisan serve",
        "build": "php artisan serve",
        "test": "php artisan test --parallel",
        "db:seed": "php artisan seed",
        "db:migrate": "php artisan migrate:fresh"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
